spring.datasource.url=jdbc:postgresql://ep-tiny-snow-awiqrx0l-pooler.c-12.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_7CoNwI3zDvpA

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

server.port=8081

# --- Resend (email) ---
resend.api-key=${RESEND_API_KEY:re_temp_placeholder}
resend.base-url=https://api.resend.com/emails
resend.from-email=noreply@contact.vativahub.com
resend.from-name=Vativa Hub LMS

# --- Password Reset Link ---
reset-link.base-url=${RESET_LINK_BASE_URL:http://localhost:4200/reset-password}
reset-link.expiry-minutes=30

# --- JWT ---
app.jwt.secret=this-is-a-temporary-dev-secret-key-please-replace-me-32chars
app.jwt.expiration-ms=86400000

# ------Actuator------
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# --- Question Bank imports ---
# This directory is application data, never a publicly served static directory.
app.question-bank.upload-directory=${QUESTION_BANK_UPLOAD_DIRECTORY:./data/question-imports}
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=101MB
